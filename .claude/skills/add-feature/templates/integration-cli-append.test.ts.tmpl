
  it('TODO: {{namespace}} {{action}} — describe expected behavior', async () => {
    const payload = (await runCli('{{namespace}}', '{{action}}' /* TODO: add args */)) as {
      success: boolean
      result?: {
        // TODO: Define expected result shape
      }
      error?: string
    }

    assert.equal(payload.success, true)
    // TODO: Assert specific fields
  })
  // ADD_ACTION: test
